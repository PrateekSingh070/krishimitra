# Base URL of the KrishiMitra Node API. Leave blank to use the Vite dev proxy
# (/api -> http://localhost:3000). Set for production builds.
VITE_API_BASE=

# Optional: bearer token for the API when AUTH is enabled (dev usually disables auth).
VITE_API_TOKEN=
